#include "factors.h"

/**
 * _find_div - Function
 * Return: void
*/
void _find_div(void)
{
    /*

    mpz_t x;

    mpz_init(x);
    mpz_set_str(LNUM, BFF, 10);
    for (x = 2; x < EX.GMP; x++)
    {
        if (EX.GMP % x == 0)
            return (x);
    }
    mpz_clear(x);
    EX._DIV = x;
    mpz_clear(_div);
    */
    return;
}
