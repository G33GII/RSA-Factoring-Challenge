#include "factors.h"

/**
 * main - main function
 * @ac:
 * @av:
 * Return:
*/
void largenum(void)
{


/*
    mpz_t LNUM, DIV_R;





    mpz_inits(LNUM, DIV_R, NULL);



        mpz_set_str(LNUM, BFF, 10);
        mpz_set_str(DIV_R, BFF, 10);

        mpz_set_str(LNUM, BFF, 10);
        mpz_set_str(DIV_R, BFF, 10);

         for (unsigned long i = starting_divisor; i <= 1000; i++) { // Loop through potential divisors
        if (mpz_divisible_ui_p(num, i)) {
            mpz_set_ui(divisor, i); // Set the divisor
            break; // Exit loop if a divisor is found
        }
    }



        mpz_div(DIV_R, LNUM, 2);

        gmp_printf("%Zd=%Zd\n", LNUM, DIV_R);


    mpz_clears(LNUM, DIV_R, NULL);
*/
return;
}









